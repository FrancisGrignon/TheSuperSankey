version: '3.3'

services:

  sankey-api:
    build:
      context: ./Services/Sankey.API/
      dockerfile: ./Dockerfile
  sankey-web:  
    build:
      context: ./Web/Sankey.Web/
      dockerfile: ./Dockerfile   
