version: '3.3'

services:

  sankey-api:
    image: sankey-api:v1
    ports:
      - "8080:80"
    networks:
      - sankey-net
  sankey-web:
    image: sankey-web:v1
    ports:
      - "8081:80"
    networks:
      - sankey-net    
  sankey-nginx:
    image: sankey-nginx:v1
    ports:
    - mode: host
      published: 80
      target: 80
    networks:
      - sankey-net           

networks:
  sankey-net: